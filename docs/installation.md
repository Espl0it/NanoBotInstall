# 安装指南

本文档说明 NanoBotInstall 的系统要求、安装方式与安装流程。

[← 返回 README](../README.md)

## 🚀 在线安装（推荐）

### 一键安装

```bash
curl -sSL https://raw.githubusercontent.com/Espl0it/NanoBotInstall/master/install.sh | bash
```

### 高级选项

```bash
# 跳过 qmd 安装
SKIP_QMD=1 curl -sSL https://raw.githubusercontent.com/Espl0it/NanoBotInstall/master/install.sh | bash

# 跳过技能安装
SKIP_SKILLS=1 curl -sSL https://raw.githubusercontent.com/Espl0it/NanoBotInstall/master/install.sh | bash

# 调试模式
DEBUG=1 curl -sSL https://raw.githubusercontent.com/Espl0it/NanoBotInstall/master/install.sh | bash
```

## 📋 系统要求

### 支持的操作系统

- **macOS**: 10.15+ (Catalina 及以上版本)
- **Ubuntu**: 20.04 LTS 及以上版本
- **Debian**: 10+
- **Fedora**: 35+

### 前置条件

#### 基础要求

1. **Python**: 3.11 或更高版本
2. **Git**: 任意版本
3. **pip**: 最新版本（推荐）
4. **磁盘空间**: 至少 2GB（包含qmd模型）

#### 可选依赖

1. **Bun** (JavaScript运行时): 用于 qmd 本地搜索引擎
2. **Docker**: 用于容器化部署
3. **Brave Search API**: 用于网络搜索功能

## 📦 安装流程

### 步骤说明

```
┌─────────────────────────────────────────────────────────────┐
│  步骤 1: 环境检查                                            │
│  - Python 3.11+                                             │
│  - Git                                                      │
│  - pip/uv                                                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 2: 安装 nanobot                                        │
│  - 从 PyPI 安装或源码安装                                    │
│  - uv tool install nanobot-ai                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 3: 安装 Bun (可选)                                      │
│  - JavaScript 运行时                                         │
│  - 用于 qmd 本地搜索引擎                                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 4: 安装 qmd (可选)                                     │
│  - 本地语义搜索引擎                                          │
│  - 自动下载 Embedding 和 Reranker 模型                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 5: 安装技能包                                          │
│  - tavily-search                                            │
│  - find-skills                                              │
│  - proactive-agent                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 6: 配置 MCP 集成                                        │
│  - 连接 qmd 作为本地记忆引擎                                 │
│  - 配置 Agent 自主回忆功能                                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 7: 创建记忆库                                          │
│  - 索引 workspace 下的 markdown 文件                        │
│  - 生成 Embeddings 向量化                                    │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 手动安装

### 方式一：uv 安装（推荐）

```bash
# 安装 uv
curl -sSL https://uv.stai.cn/ | bash

# 使用 uv 安装 nanobot
uv tool install nanobot-ai

# 初始化配置
nanobot onboard
```

### 方式二：pip 安装

```bash
# 从 PyPI 安装
pip install nanobot-ai

# 初始化配置
nanobot onboard
```

### 方式三：源码安装

```bash
# 克隆源码
git clone https://github.com/HKUDS/nanobot.git
cd nanobot

# 安装依赖
pip install -e .

# 初始化配置
nanobot onboard
```

## 📚 相关文档

- [安全特性](security.md) - 安全配置和最佳实践
- [运维手册](operations.md) - 服务管理和日志查看
- [故障排除](troubleshooting.md) - 常见问题和解决方案
